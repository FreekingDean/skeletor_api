require 'spec_helper'

describe SkeletorApi do
  it 'has a version number' do
    expect(SkeletorApi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
